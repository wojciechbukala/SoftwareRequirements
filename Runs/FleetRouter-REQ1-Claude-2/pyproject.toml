[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fleetrouter"
version = "1.0.0"
description = "Daily route planner for courier companies"
requires-python = ">=3.8"

[project.scripts]
fleetrouter = "fleetrouter.__main__:main"
